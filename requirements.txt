# financedata is installed separately (shared local library — not on PyPI yet).
# Local:  pip install -e ../FinanceData
# Pi:     pip install -e ../FinanceData  (handled in deploy/deploy.sh)

# AI / LLM
anthropic>=0.40.0
openai>=1.50.0
dspy-ai>=2.5.0
# MIPROv2's Bayesian search backend. Not pulled in by dspy-ai, and the import
# only fires at the end of compile() — after demo bootstrapping and instruction
# proposal have already spent several minutes of heavy prompt-model calls. A
# weekly run got that far and died there for want of this line.
optuna>=3.6.0

# Data
yfinance>=0.2.40
alpha_vantage>=2.3.1
httpx>=0.27.0
feedparser>=6.0.11

# Technical Analysis
ta>=0.11.0
numpy>=1.26.0
scipy>=1.13.0

# Database
sqlalchemy>=2.0.0

# Web / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
websockets>=13.0
python-multipart>=0.0.9

# Scheduling
apscheduler>=3.10.4
pytz>=2024.1
exchange-calendars>=4.5

# Config
python-dotenv>=1.0.0
pydantic-settings>=2.4.0
pydantic>=2.8.0
